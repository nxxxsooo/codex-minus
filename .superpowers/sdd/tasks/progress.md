# SDD ledger — plan: /Users/mingjian/Documents/sync/GitHub/codex-minus/.worktrees/native-capability-priority/openspec/changes/prioritize-native-capabilities-for-mixed-providers/tasks.md

- Integration prerequisites 1.1–1.3, 1.7–1.8: complete at `2292c25`; raw catalog tip `60087e2`, onboarding source `1843af7`; baseline 35 TypeScript pass, 67 Rust pass, 1 ignored.
- Pending first implementation batch: 1.4–1.6, the unified provider-detail request and draft-aware catalog planner.
- Task 1: fix round 1/5 (5 findings addressed, 3 open — canonical `modelInsertMode`/strict non-empty `authContents`; draftless external pointer bypass; editable catalog draft not carried by current Save; commit `784bc34`).
- Task 1: fix round 2/5 (2 findings addressed, 1 open — external pointer equality still trims quoted string bytes; commit `58306f1`).
- Task 1: fix round 3/5 (1 finding addressed, 0 open — external pointer bytes preserved exactly; commit `e74e637`).
- Task 1: complete (commits `22ad805..e74e637`, review clean).
- Task 2: fix round 1/5 pending — command-boundary inspection normalizes raw settings through pinned core and swallows malformed JSON; must use strict read-only raw decode and retain evaluator evidence. Minor deferred: future catalog-state version guard; more precise malformed TOML shape reasons.
- Task 2: fix round 1/5 (2 findings addressed, 0 open — raw settings inspection and malformed-input failure; commit `5789634`).
- Task 2: complete (commits `c4e5961..5789634`, review clean; deferred Minor: future catalog-state version guard and malformed TOML reason precision).
- Task 3: fix round 1/5 pending — reject/scrub `authContents`; revision must only echo; malformed actor headers must fail closed; preserve matching bearer bytes; prove pure-transform command has no storage effects. Ruling: local masked provider-detail draft IPC may contain the provider bearer/structured editor key as explicitly allowed by design, but never `authContents`, OAuth, status/evidence, logs, or error payloads. Minor deferred: identical legacy-table formatting selection.
- Task 3: fix round 1/5 (6 findings addressed, 0 open — auth ownership, nested local draft boundary, revision echo, malformed exit headers, bearer preservation, audited read-only adapter; commit `ae0fd63`).
- Task 3: complete (commits `85f98e2..ae0fd63`, review clean; deferred Minor: legacy semantic-identical formatting and superseded wording in local task report).
- Task 4a (OpenSpec 4.1–4.4): initial implementation `2894df2`; independent review rejected four issues: compatibility/pure-API structured/raw conflict bypass, provider secrets in failure payloads, substring-based stale misclassification, and accidental Task 7.6/7.7 runtime-fingerprint expansion.
- Task 4a: fix round 1/5 (4 findings addressed, re-review pending). Strict reconciliation now runs before evaluator state short-circuits and covers Base URL/model/key conflicts plus reserved selection across native, legacy-upgrade, and pure-API paths; failures carry explicit typed categories and omit settings/draft secrets; future runtime fingerprint/generation schema was removed and active commits atomically set only the existing restart marker. Focused 7/7, Rust 92 pass + 1 approved-only ignore, frontend 42/42, TypeScript check, fmt, diff check, and strict OpenSpec validation pass. No 4.5+ failure injection, Context matrix, auth-race, generic-save hardening, or caller migration was implemented.
- Task 4a: fix round 2/5 (1 finding addressed, re-review pending). Nonblank structured/raw Base URL, model, and provider bearer values now compare exact bytes instead of trimming; only the missing-value decision trims. Added byte-distinct zero-write regressions for native Base URL, legacy model, and pure-API bearer. Focused 8/8, Rust 92 pass + 1 approved-only ignore, frontend 42/42, TypeScript check, fmt, diff check, and strict OpenSpec validation pass.
- Task 4a: complete (commits `2894df2`, `142e4a2`, `ccced85`; fix-round 2 independent review approved clean within OpenSpec 4.1–4.4, zero blockers). Deferred exactly as planned: 4.5–4.13 failure matrices, auth race/gates/audits, generic-save hardening/caller migration, and frontend wiring.
- Task 4b / 4.5a: complete at `5c0b383` — added a hidden production-no-op normalization checkpoint; injected failure returns a sanitized `InvalidDraft` and preserves the complete isolated app-state/Codex-home file generation byte-for-byte. RED was missing checkpoint API; focused 1/1, fmt, and diff checks pass. Fresh scoped review approved spec and quality with zero findings. OpenSpec 4.5 remains unchecked until every named failure stage is covered.
- Task 4b / 4.5a deployment: complete — `npm run build` completed successfully in a traceable managed session (the runtime reaped two attempted orphaned `nohup` launches before `npm` execution); bundle version 0.3.0 passed strict deep code-sign verification and was installed as `/Applications/Codex-- Manager.app`. The prior unchanged 0.4.0-rc.1 was moved to `/Applications/Codex-- Manager.previous.app`; no auth/config was touched.
- Task 4b / 4.5b: complete at `fc16bea` — added the catalog-materialization checkpoint after real composition/hash/path mutation planning but before journal mutation; injected failure is sanitized as `CatalogUnavailable` and preserves the complete file generation. RED was the missing checkpoint variant; injected matrix 2/2, fmt, and diff checks pass. Fresh scoped review approved spec and quality with zero findings. OpenSpec 4.5 remains unchecked.
- Task 4b / 4.5b deployment: complete — `/Applications/Codex-- Manager.app` contains `fc16bea` (binary SHA-256 `329855d2b2af558f0b94c7ea98a143a98806e17e17d846c33f714d59ee0eb241`), version 0.3.0, strict deep code-sign valid. Original 0.4.0-rc.1 remains `.previous.app`; `5c0b383` remains `.rolling.app`; no auth/config touched.
- Task 4b / 4.5c: GREEN pending scoped review — extended the production journal with a no-op-by-default after-apply observer and injected failure immediately after verified settings persistence. The real transaction rolls back all manager-owned files to the byte-identical prior generation and returns sanitized `TransactionFailed`. RED was the missing checkpoint variant; injected matrix 3/3, fmt, and diff checks pass. OpenSpec 4.5 remains unchecked.
- Task 4b / 4.5c: fresh scoped review approved spec and quality with zero findings at `8bf1b23`; reviewer confirmed failure before `applied_count` persistence remains safe because synchronous rollback and recovery operate on the complete journal／actual target hashes. Build／deploy deferred while the user's app is running; current installed app remains `fc16bea`.
- UI acceptance handoff: the disabled catalog-mode buttons labeled “统一保存接入后可编辑” are temporary only. Task 5/6 must restore them as editable controlled draft inputs after frontend unified-commit integration; they persist only through main Save／SetCurrent, never a standalone catalog save. This does not alter Task 4b scope.
- Catalog diagnosis handoff: the observed “当前 · 4/5” list is consistent with the profile's explicit `native-official` mode; the user's expected 8-model effective list belongs to later `official-plus-custom` unified-save UI integration (official baseline including `codex-auto-review` plus three custom models). Do not change model/UI ownership in Task 4b.
- Task 4b / 4.5d: GREEN pending scoped review — injected failure immediately after live `config.toml` is atomically applied and target-verified. The real journal restores the complete prior file generation and returns sanitized `TransactionFailed`. RED was the missing checkpoint variant; injected matrix 4/4, fmt, and diff checks pass. Build／deploy intentionally deferred while the user's app runs; OpenSpec 4.5 remains unchecked.
- Task 4b / 4.5d: fresh scoped review approved spec and quality at `4f36901` with zero findings; exact live path, active-only dispatch, post-write/hash/permission placement, real rollback, and sanitized error were confirmed. Build／deploy remains deferred while the user's app runs.
- Task 4b / 4.5e: GREEN pending scoped review — after the real protected Context table verification succeeds, an injected verification-stage failure enters the same journal rollback and restores the complete prior file generation. The shared observer uses interior mutability only to sequence two synchronous transaction callbacks; production remains no-op. RED was the missing checkpoint; injected matrix 5/5, fmt, and diff checks pass. Build／deploy deferred; OpenSpec 4.5 remains unchecked.
- Task 4b / 4.5e: fresh scoped review approved spec and quality at `29ec584` with zero findings; actual verifier ordering, active-only dispatch, rollback, sanitization, and stack-local synchronous `RefCell` behavior were confirmed. Build／deploy remains deferred.
- Task 4b / 4.5f: GREEN pending scoped review — added a final observation after the journal is marked committed and every target completes final verification, but before cleanup. Injected failure still enters real rollback and restores the complete prior generation. RED was the missing checkpoint; all six authoritative 4.5 failure stages now pass in the injected matrix 6/6, with fmt/diff clean. OpenSpec 4.5 awaits scoped approval before checkbox completion; build／deploy deferred.
- Task 4b / 4.5f and OpenSpec 4.5: complete at `974fc19`; fresh scoped review approved spec and quality with zero findings. All six authoritative failure stages use production planner/journal paths, typed sanitized errors, real rollback where mutation occurred, and byte-identical complete-generation assertions. Checkbox completed after review; build／deploy remains deferred while the user's app runs.
- Task 4b / 4.6a: GREEN pending scoped review — a successful active commit now has an explicit semantic TOML assertion for complete `mcp_servers`／`skills`／`plugins` identity, observes exactly one real Context verification, and proves live `auth.json` byte identity with no race. This is test-only coverage of the existing protection boundary. RED was the missing semantic snapshot helper; focused 1/1, fmt, and diff checks pass. OpenSpec 4.6 remains unchecked until failure coverage is separately reviewed; build／deploy deferred.
- Task 4b / 4.6a: fresh scoped review approved spec and quality at `39f595e` with zero findings; semantic deserialization, real active transaction, exactly-one checkpoint, and raw auth-byte assertions were confirmed. Build／deploy remains deferred.
- Task 4b / 4.6b: GREEN pending scoped review — the real Context-verification injected failure now has explicit semantic three-table equality and exact no-race auth-byte assertions in addition to the complete-generation byte snapshot. Test-only; RED was missing before-state fixture evidence, and the focused success/failure Context pair passes 2/2 with fmt/diff clean. OpenSpec 4.6 awaits scoped approval; build／deploy deferred.
- Task 4b / OpenSpec 4.6: complete at `f9be248`; fresh scoped review approved 4.6b spec and quality with zero findings. Success and actual rollback failure both prove full semantic identity of all three protected Context tables and exact no-race auth bytes; checkbox completed after review. Build／deploy remains deferred.
- Task 4b / 4.8: GREEN pending scoped review — a hidden checkpoint runs immediately before the production auth-generation equality check. The test callback, representing the official client rather than Manager storage, writes newer isolated auth bytes and returns success; the production check detects drift, aborts, and rolls back all non-auth files byte-for-byte while preserving the newer auth. Error is typed/sanitized. RED was the missing checkpoint; auth focused pair 2/2, fmt, and diff checks pass. OpenSpec 4.8 remains unchecked until review; build／deploy deferred.
- Task 4b / OpenSpec 4.8: complete at `d0bc6fa`; fresh scoped review approved spec and quality with zero findings. Only the isolated test callback writes newer auth; production remains read-only/no-op, exact auth exclusion is narrow, every other file rolls back byte-for-byte, and newer auth survives. Checkbox completed after review; build／deploy deferred.
- Task 4b / 4.7: GREEN pending scoped review — a real active commit semantically compares the complete non-provider root map before/after, covering live review model, reasoning effort, response storage, sandbox/network policy, acknowledgements, feature flags, Context, and unrelated tables. The draft injects conflicting global sentinels plus a fake Context entry; none survives into live or persisted profile config. Test-only; RED was the missing unrelated-root semantic snapshot helper; focused 1/1, fmt, and diff checks pass. OpenSpec 4.7 remains unchecked until review; build／deploy deferred.
- Task 4b / 4.7 fix round 1/5 pending re-review — initial review found two Important test-evidence gaps: provider-owned staged fields were not asserted, and persisted-profile inspection used a normalizing fixture helper. Fix now parses the selected provider table in both live and raw persisted config, asserts changed Base URL／bearer, and deserializes raw `settings.json` without normalization before checking global absence. RED was missing raw/selected-provider helpers; focused 1/1, fmt, and diff pass.
- Task 4b / 4.7 fix round 1/5 (2 addressed, 0 open; commit `3e0b16c`) — original reviewer approved: live and raw-persisted provider values are explicitly asserted, and raw `settings.json` inspection bypasses all fixture normalization. No new Critical／Important breakage.
- Task 4b / OpenSpec 4.5–4.8: implementation complete pending broad combined review. Every mini-slice passed RED→GREEN and scoped review; 4.5／4.6／4.8 were already checked after review, and 4.7 is now checked after its fix re-review. Build／deploy remains deferred while the user's app runs; current installed app remains `fc16bea`.
- Task 4b combined pre-review verification: complete provider transaction suite 17/17, `cargo fmt --check`, `git diff --check`, and strict OpenSpec validation pass.
- Task 4b combined review: complete and approved at tracked HEAD `98f0ebc`; fresh reviewer returned spec ✅／quality ✅ with zero Critical／Important／Minor. Review covered all exact Task 4b commits and excluded unrelated `eca760c`. OpenSpec 4.5–4.8 are complete. Build／deploy remains deferred while the user's app runs; current installed app remains `fc16bea`.
- Task 4c / OpenSpec 4.9: fix round 1/5 (2 addressed, 0 open; commits `55e25e5`, `3220094`) — the first review found a Critical load-time migration bypass and an Important test-entry gap. The corrected production load path now uses the same exact API-key-only parser before normalization, rejects OAuth／additional fields and structured/raw conflicts before any write, projects the sole persisted legacy key into the provider bearer, deletes the copy, and keeps errors secret-free. Fresh scoped re-review approved spec and quality with zero Critical／Important. Deferred Minor: dedicated SetCurrent and each key-conflict permutation remain coverage-only gaps; shared validation is action-independent and exact-conflict checks are implemented. Focused provider transaction suite 18/18, direct load-time regression, fmt, and diff checks pass.
- Task 4c / OpenSpec 4.9: complete (commits `55e25e5`, `3220094`; fix-round 1 review clean aside from one deferred coverage Minor).
- Task 4c / OpenSpec 4.10: complete (commits `72de28a`, `f4fcf4b`, `4ac8430`; fix-round 2 review clean with zero findings). Active Save／SetCurrent now require current official auth plus matching target, account, and workspace scope before catalog planning or staging. Production target verification cannot be injected, inactive action-required Save remains allowed, and the final auth-generation check is bound to the bytes captured before scope validation so a concurrent official auth change preserves the newer auth while rolling back Manager-owned files. Focused provider transaction suite 20/20, non-test `cargo check`, fmt, and diff checks pass; build／deploy remain deferred.
- Task 4c / OpenSpec 4.11: complete (commits `c5e3d00`, `e38b3f8`, `e62f6a9`, `18225ca`; fix-round 2 review clean with zero findings). A real provider transaction proves journal JSON contains no OAuth／provider-key payload, every key-bearing file is `0600`, every containing directory is `0700`, and rollback cleans journal／staging material. Provider probes, Doctor, model evidence, live-config display, auth status, errors, and every Rust diagnostic writer now use fail-closed structured redaction; arbitrary response bodies are not surfaced, identities are hashed, account labels are generic, model IDs are sanitized before evidence persistence, and only exact safe event fields／enums survive. The explicitly allowed local masked provider-detail draft／settings IPC bearer remains unchanged. Focused compatibility 7/7, transaction 21/21, live-state 7/7, `cargo check`, fmt, and diff checks pass; build／deploy remain deferred.
- Task 4c / OpenSpec 4.12a: complete (commits `0196731`, `da8781b`, `2dced66`, `93e5ff5`; final review clean with zero Critical／Important／Minor). Generic `save_settings` now accepts only a byte-captured raw provider topology or the exact UI canonical projection derived from that same capture, re-grafts the complete raw provider-owned JSON, allows unrelated settings and first-run default saves, rejects every provider-bearing difference, and performs a pre-commit generation check that preserves concurrent provider writes. Active aggregate／legacy projections are mirrored, provider auth remains prohibited, and typed static errors distinguish provider diff, invalid persisted state, concurrent generation change, and secure transaction failure. Focused generic-save 5/5, provider transaction 26/26, `cargo check`, fmt, and diff checks pass. OpenSpec 4.12 remains unchecked pending caller migration.
- Task 4c / OpenSpec 4.12: complete through `0d1126c`; final scoped reviews are clean with zero Critical／Important／Minor. All enablement, reorder, copy, delete, aggregate cleanup, test-model, detail Save, and SetCurrent UI writes now use one ProviderCommit envelope and CAS fingerprint. Legacy switch／save-active／apply／clear／direct-live／backfill and ordinary standalone catalog commands are absent from the Tauri handler; explicit source-hash-bound external adoption remains. Existing catalog state is fail-closed when unavailable, current deletion is disabled and backend-rejected, current responses use a tested revision/baseline state machine, complete catalog controls persist only through unified Save／SetCurrent, and managed Context raw／structured／active-live conflicts require explicit confirmation before one atomic cleanup generation. Frontend 50/50, provider transaction 31/31, `cargo check`, TypeScript check, fmt, and diff checks pass. Build／deploy remain deferred.
- Task 4c / OpenSpec 4.13: complete through `3d94ec2`; scoped and cumulative `98f0ebc..3d94ec2` reviews pass Spec／Quality with zero Critical／Important／Minor. Direct generic-save invocation rejects invalid provider TOML, actor／bearer／auth changes, non-empty `authContents`, active-ID, topology, common／context, legacy, and test-model differences with the complete file generation unchanged. Active／inactive detail and topology requests cannot use `draftRevision` to bypass a stale fingerprint. ProviderCommit now compares the initially captured raw settings bytes immediately before the journal transaction and preserves a concurrent generation on typed `StaleState`. List topology stays on persisted UI state until success and reconciles a current failure to any newer baseline adopted from an older successful response, without disturbing ignored stale responses or detail drafts. Full Rust library 128 passed／1 live-OAuth ignored, provider transaction 33／33, frontend 51／51, checks／fmt／diff pass. No Tauri package, install, deployment, live auth/network/image probe, or release was performed.
- Task 5a / OpenSpec 5.1–5.7: complete through `9247ed7`; all scoped reviews pass Spec／Quality with zero final Critical／Important／Minor. Brand-new empty mixed providers use the canonical native-priority contract, existing TOML mutations preserve the native table or use a revisioned backend transform, provider-scoped goals ownership is removed, and response-only inspection plus raw-draft validation is session／profile／revision correlated and fail closed. New-provider raw TOML is read-only until first combined Save; structured canonical fields remain the sole first-save path. Frontend 84／84, provider native-capability Rust suite 20／20, TypeScript／Rust checks, fmt, and diff pass. OpenSpec 5.8 and Task 6 remain pending; no build／deploy／live mutation was performed.
- Task 5b / OpenSpec 5.8: complete through `0189c3b`; final fix review passes Spec／Quality with zero Critical／Important／Minor. Responses-to-Chat and other compatibility exits use a revisioned backend preview with exact typed confirmation, unsupported key／actor blockers fail closed, and Save／SetCurrent cannot commit a pending preview. Production transformation derives catalog ownership from persisted settings and catalog state, so external profiles cannot forge a managed mode; missing, invalid, newer-version, or mismatched ownership also fails closed before preview. Frontend 89／89, provider native-capability Rust suite 24／24, full Rust library 128 passed／1 approved live-OAuth ignore, TypeScript／Rust checks, fmt, and diff pass. Task 5 is complete; Task 6 remains pending. No build／deploy／live mutation was performed.
- Task 6a / OpenSpec 6.1–6.4: complete through `e7490fb`; all scoped and fix reviews pass Spec／Quality with zero final Critical／Important／Minor. Read-only inspection feeds response-only provider-detail state; upgrade, compatibility exit, Actor replacement, and legacy-ID migration are explicit revisioned draft transformations. Legacy collisions require an unused non-reserved ID, conflicting Actor replacement requires its exact confirmation, external ownership remains fail closed, and cancel／navigation／close have zero persistence effects. Current transport errors restore only the matching session／profile／revision resolution state, while stale errors remain ignored; Save／SetCurrent stay blocked until resolution completes. Frontend 106／106, evaluator 14／14, draft transformer 24／24, TypeScript check, Rust fmt, and diff checks pass. No build／deploy／network／live mutation was performed; OpenSpec 6.5–6.9 remain pending.
- Task 6b / OpenSpec 6.5–6.6: complete through `14c6b24`; final backend and frontend reviews pass Spec／Quality with zero Critical／Important／Minor. A coordinator-bound, single-snapshot backend producer emits only redacted independent provider, OAuth-session, plan, Actor, catalog/model, upstream, runtime, route, and target-policy evidence. The App consumes that response only while the full authoritative profile/catalog source and response-only evidence fingerprint match; latest-request sequencing clears failed/stale observations, and the ledger never enters settings, profile drafts, transforms, or ProviderCommit. Target/path truth tables keep Free／Paid／unknown plans descriptive unless an exact backend-verified rule applies, keep row evidence independent, and treat Actor as eligibility only. Frontend 116／116, backend evidence 2／2, TypeScript／Rust checks, fmt, diff, and scoped reviews pass. No bundle／deploy／network／live mutation was performed; OpenSpec 6.7–6.9 remain pending.
