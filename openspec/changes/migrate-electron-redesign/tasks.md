# Tasks

This is the executable checklist for the Build Contract confirmed by the user on 2026-09-22 (“确认，开始实现”). The first implemented provider workflow is a review milestone before the remaining-page migration.

## 1. Runtime and isolated backend

- [x] 1.1 Detach Rust command wrappers from Tauri and add the standalone backend executable with a static command allowlist; verify compilation, actual protocol round trips, rejection of unknown/bypass commands and preserved provider registration tests.
- [x] 1.2 Add bounded NDJSON framing, correlated responses and static transport errors; verify malformed/oversized frames, request panic/death, response correlation and EOF shutdown without secret-bearing output.
- [x] 1.3 Preserve the legacy single-writer guard and transaction recovery, and validate isolated test paths before startup; verify competing guards fail closed and a temporary fixture cannot resolve outside its requested home.
- [x] 1.4 Add Electron main/preload, trusted local asset loading, IPC sender validation, backend supervision and lifecycle controls; verify unauthorized IPC, backend failure settlement, native instance activation and close/show/quit behavior.

## 2. Representative provider workflow

- [x] 2.1 Replace renderer Tauri imports with the narrow desktop adapter and explicit backend connection state; verify type checks, domain response compatibility, failure notices and no automatic replay of mutations.
- [x] 2.2 Implement Bauhaus shared tokens/navigation and the B01–B06 provider workflow with model editing, preserving draft/CAS/catalog rules; verify existing frontend regressions and the App shell budget.
- [x] 2.3 Exercise real Electron → IPC → Rust provider load/edit/save/activate against isolated fixtures; verify Context, OAuth and external-file sentinels, inactive save/routing-disabled behavior, stale-save rejection and loss/reconnect handling.
- [x] 2.4 Present the implemented provider workflow at 1180 × 820 and 960 × 720 for the user milestone review; verify readable content, keyboard focus, dialog behavior, native controls and light/dark presentation, then record approval before task group 3.

## 3. Remaining application workflows

- [x] 3.0 Integrate the two still-open feature branches locally into `codex/electron-redesign`, preserving the uncommitted work and provenance, updating the Electron cleanup RPC allowlist and image/long-context controls; verify merge ancestry, worktree snapshot recovery, combined Rust/frontend tests, and isolated real IPC cleanup/feature materialization.
- [x] 3.1 Migrate session active/archive lists, multiselection, policy preview, partial failures and provider adaptation to the shared design, including PR #48's permanent single/selected deletion and clear-all-archives behavior; verify existing session rules plus real isolated archive/restore/delete, no-backup cleanup and selection workflows.
- [x] 3.2 Complete Provider Doctor, auth/config panels, model/ownership states, PR #49's long-context and explicit image controls, and transition confirmations; verify diagnostic row independence, sanitized details, protected fields, image-feature leaf ownership and catalog/transition regression tests.
- [x] 3.3 Complete preferences, shared accessible dialogs/notices, localization and responsive/reduced-motion behavior; verify Chinese/English and dark/light states with no unreachable or obscured actions at supported sizes.

## 4. Distribution and updates

- [x] 4.1 Implement the Electron-marked update manifest, bounded downloads and pinned minisign verification; verify invalid signatures, wrong runtime/platform/architecture, malformed feeds and downgrade rejection before extraction/execution.
- [ ] 4.2 Implement macOS staged installation/rollback and verified Windows installer handoff; verify with disposable application trees/install stubs, interruption/failure cases and explicit manager-versus-Codex restart behavior.
- [ ] 4.3 Add matching backend resources to Electron packaging and migrate version/release/CI configuration; verify local package contents, version consistency, platform build commands and absence of Tauri runtime dependencies.
- [ ] 4.4 Deliver the user-selected v0.5.0 automatic Tauri→Electron upgrade: generate the legacy feed from the same signed artifacts, verify the pinned old updater against a disposable macOS bundle, and verify Windows legacy registry/installer-argument handoff on the native x64 CI host before publication.

## 5. Delivery verification

- [x] 5.1 Run `npm run verify`, frontend build, full Rust tests and focused Electron/IPC/update checks; record actual results and distinguish unavailable native Windows verification from passing compilation.
- [x] 5.2 Build and launch the local macOS review package against isolated data; verify packaged assets/backend discovery and critical routes, report measured package/startup characteristics without unsupported claims, and deliver the review build within the agreed publication/installation authority.

## Evidence at the first provider review

- `npm run verify`: 254 frontend tests and 10 desktop transport tests pass, along with TypeScript and knip. `npm run vite:build` passes.
- `cargo test`: 352 tests pass (306 unit, 44 capability integration, 2 real stdio-process integration).
- `scripts/desktop-smoke.mjs`: real Electron/preload/Rust fixture verifies inactive save, atomic dirty-draft activation, Context/auth/external-file preservation, killed-core draft retention/reconnect/save, stale-save rejection, routing-disabled save, 1180 × 820 and 960 × 720 layouts, and English/dark appearance. Uses disposable homes only.
- Runtime screenshots and `runtime-evidence.json`: `/Users/mingjian/Documents/sync/Designs/personal/codex-minus/implementation/`.
- User review found uneven control dimensions/text wrapping and insufficient model-section padding. Corrected the radio explanation grid, 36px form actions/32px row controls/16px icons, wrapping candidate groups, and model-region spacing (20px outer, 16px section gap, 12px × 16px row padding), then confirmed computed styles in Electron.
- The update adapter deliberately reports review-build unavailability until tasks 4.1–4.2 implement verified updates. Native Windows lifecycle, packaged install/update/rollback, final provider milestone approval and the remaining screen families are still pending.
- On 2026-09-23 the user explicitly approved the supplier workflow review milestone and asked to continue the remaining interface families. The latest runtime screenshots above reflect the corrected model-area padding and control sizing; task group 3 is now authorized.
- After the approval, an isolated main-process inspector exercise confirmed close-to-hide retains the same window and unsaved editor, second launch reopens it without taking focus, and explicit quit drains the core before exiting. Automation now places native windows on a detected secondary work area with `showInactive`; without an adequate secondary display, native visual checks fail closed. The workspace-wide rule lives in `~/Documents/sync/AGENTS.md`.
- Local integration of PR #48 (`4f70f86`) and PR #49 (`0607d40`) landed as merge commits `277be8d` and `14edd23` on `codex/electron-redesign`, not on remote `master`. The pre-integration uncommitted work was snapshotted and reapplied with conflict resolution; all 61 snapshotted files remain present, with overlapping files intentionally changed by integration. The saved stash remains available until cleanup. The Electron dispatcher/allowlist now exposes the permanent cleanup command instead of the retired backup-based delete command. Full Rust tests ran after the merge; later frontend counts and real Electron evidence are recorded below. Session UI/packaged runtime work is still tracked by the unfinished tasks above.
- Post-integration review caught a new-provider target round trip that left generated official-slug custom rows with empty reasoning levels, and cleanup notices overwritten by refresh failures. Focused red/green tests now cover baseline capabilities, pre-existing user-owned and provider-candidate rows, existing official overrides, materialized-but-unsaved target changes, per-session failure details and stale-list guidance. Verified with `npm run verify` (284 frontend and 11 desktop tests), frontend build and isolated Electron smoke covering pure-API image enablement, permanent single/clear-all-archived deletion with active-row and no-backup sentinels, provider flow and secondary-display/no-focus lifecycle. Full Rust tests were run after the merge (317 library + 49 integration) and unchanged Rust source remains under `cargo fmt --all --check`.
- The session layout was checked on the secondary-display Electron review window at 960 × 720 in Chinese/light and English/light plus English/dark. Geometry checks reject action controls that overlap the update column or clip their labels; active, archived, selected and clear-all-confirmation captures live in the implementation evidence directory. The isolated Electron → preload → Rust smoke exercises cancelled and confirmed single deletion, all-archived deletion, stale scan rejection, active-only provider adaptation with an untouched archived-file sentinel, read-only archive preview and cancelled policy enablement. A disposable target-CLI fixture now executes actual archive and restore command routing plus postcondition validation; it modifies only the isolated session database and rollout. The smoke also asserts the old backup-based delete command remains denied.
- On 2026-09-23 the user chose to publish the completed Electron application rather than cut a separate interim Tauri release. The trial Tauri release worktree/branch was removed without any push or tag. Publication remains gated by 3.1–5.2 and native Windows verification limits; pushing a review branch does not make a release.
- Provider Doctor's real Electron → IPC → Rust test uses a controlled loopback Responses server: empty `/models` with successful text, then a valid `/models` list with a rejected text request. Its four rows remain independent, the actionable recommendation stays within Responses, and an echoed fixture bearer is absent from the displayed result. Backend death now renders an unfinished diagnosis rather than claiming completion; the completed progress bar no longer lags behind its aria value. The live config panel is read-only, redacts the bearer and retains visible protected Context. The focused active pure-API profile independently disables and re-enables `features.image_generation` through draft transforms and commits, preserving Context and auth bytes. Existing catalog/external-owner, revisioned transition, and image-leaf failure-injection suites cover the remaining ownership gates; `npm run verify` (284 frontend + 11 desktop tests) and isolated Electron smoke pass after the UI changes.
- B23's existing preference controls now have a single dialog alongside the header shortcuts: light/dark, Chinese/English, current version, manual update check and explicitly confirmed Codex restart. Provider Doctor and destructive confirmation dialogs use a shared focus hook with accessible titles, Tab containment, Escape handling and return focus after busy buttons re-enable. Persistent failure notices announce as alerts with a named dismiss action. Real isolated Electron checks at 960 × 720 cover both themes, both languages, modal boundaries, Tab/Escape/focus restoration, and reduced-motion animation suppression; the latest `npm run verify` passes 284 frontend and 11 desktop tests and `npm run vite:build` succeeds. The updater remains deliberately unavailable until 4.1–4.2.
- Update checks now authenticate raw Electron manifest bytes and artifacts with the existing pinned key; strict selectors bind runtime, platform, architecture, version/tag, size and SHA-256. Bounded download tests reject truncation/overflow/tampering before returning a candidate, Rust known-signature vectors reject changed bytes even with a recomputed hash, and the private verifier CLI rejects silently. An independent read-only review led to additional tests/fixes for canonical staging ancestors, post-check rejection rollback and owner-checked staging cleanup. These are covered by the focused 25-case updater/archive/manifest suite.
- On 2026-09-23 the user chose **v0.5.0 plus automatic upgrade from Tauri**, adding task 4.4. The test-only pinned `tauri-plugin-updater@2.10.1` successfully downloaded, verified and installed the actual v0.5.0 Electron archive into a disposable macOS app directory, leaving auth/config/settings sentinels byte-identical and opening zero windows. This exposed and fixed AppleDouble tar metadata incompatibility (`COPYFILE_DISABLE=1`, no xattrs). Windows legacy NSIS argument/path/registry acceptance is configured for native x64 CI and remains unverified until that job runs.
- Real macOS package construction exposed concatenated electron-builder resource arrays that copied two core generations onto one destination. Packaging now supplies one fully resolved config file, one target-matched core, and the original product bundle/executable names. A v0.5.0 bundle passed native Electron Node-mode and Rust stdio startup checks; its renderer ASAR is about 1 MB and the app about 299 MB. Final package/UI acceptance follows the last reviewed changes.
- Final local v0.5.0 checks: `npm run verify` passed 285 frontend and 38 desktop/release tests; full Rust tests passed 319 library + 51 integration tests. The signed macOS package booted its own Electron runtime and Rust core. Its actual packaged provider/session/Doctor/preferences flows passed in offscreen headless mode with isolated state, including an explicitly awaited cancelled native-confirmation boundary, no automatic session-policy write, keyboard containment/focus return, themes/locales, backend death/reconnect, Context/auth sentinels, and orderly quit. One instrumented run reached renderer/core readiness in 2775 ms; app footprint was about 299 MB, ZIP about 128 MB. Screenshots and `headless-runtime-evidence.json` are under the established Designs implementation directory.
- Native visual limitation: the secondary display disconnected before the final package check, so the native review correctly refused to launch. The headless run never showed a window and does not count as final native second-launch focus verification. Earlier native supplier/UI approval remains recorded above. Official Codex CLI archive/restore compatibility is not claimed by the fixture-CLI smoke. Windows x64 installation/legacy upgrade is a remaining CI gate; Windows ARM64 is build/resource verification only.
- The updater review's original three Important findings and two cleanup retry follow-ups are closed: canonical containment, rejected-candidate rollback, per-attempt receipts, owner-marker-last cleanup, locked-file retry and deferred older-generation cleanup all have regression evidence. No critical/important findings remain within that review's updater scope.
- Initial PR CI exposed environment-specific verification issues: the helper test must retain its own child reference on Node 22, PR packaging needs an explicit public ad-hoc signature because builder skips its signing hook, and Windows Known Folder resolution intentionally ignores HOME/USERPROFILE substitution. Windows now tests that rejection explicitly and runs positive core/package checks only in a fresh disposable CI profile after asserting both Codex data directories are absent; the production isolation gate is unchanged. The native NSIS compile also identified and corrected quote escaping in the legacy handoff hook.
